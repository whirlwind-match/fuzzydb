target/

# Ignore eclipse project artifacts and instead rely on pom.xml
# NOTE: Currently have these committed for many projects, but want to migrate to just pom.xml 
.classpath
.settings/
.project

# Remnant of OSGi project configs under Eclipse - should eventually not generate a bin directory.
bin/
